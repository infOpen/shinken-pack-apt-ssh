# shinken-pack-apt-ssh

Shinken pack to check apt updates, via SSH.

## Prerequites

* This plugin need nagios-plugins installed on target host.
* The $_HOSTIP_VERSION$ and $_HOSTNAGIOS_PLUGINS_DIR$ should be defined in the
configuration
